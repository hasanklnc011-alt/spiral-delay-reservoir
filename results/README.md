# Results

Human-readable results are summarized in `docs/05_results.md`. Structured JSON records remain in `archive/canonical-project-records/`. Raw HDF5 simulation artifacts are excluded because they are large, generated binaries; see `archive/README.md`.
