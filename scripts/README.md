# Scripts

The executable research scripts are preserved in `archive/canonical-project-records/`. This staging repository does not add or modify simulation code. Consult the source snapshot's README and phase documents before executing any script.
