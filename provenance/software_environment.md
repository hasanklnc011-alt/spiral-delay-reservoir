# Software environment

- Operating system: Windows (version not recorded here).
- Connected Blender session: Blender `5.2.1 LTS`.
- Scene units: metric, metres, scale length 1.
- Render engine reported by the session: `BLENDER_EEVEE`.
- Python requirement in the copied project record: `>=3.11`.
- Declared Python dependencies: NumPy `>=2.0`, Matplotlib `>=3.8`.
- External simulator references exist in the canonical record; credentials and cloud configuration are not included.

All local absolute paths have been omitted or made repository-relative.
