# Blender assets

`scenes/mcp.blend` is a byte-identical archival copy of the designated canonical file. It was not modified.

`renders/p6_overview_session_render.png` was rendered from the connected Blender P6 session using its existing scene state. The session did not report a saved filepath, so its exact identity with `mcp.blend` is **unverified**. No geometry, material, camera, or light was modified to create this archive render.
